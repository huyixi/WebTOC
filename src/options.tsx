// src/options.tsx
import React from 'react';
import ReactDOM from 'react-dom';

const Options = () => {
  return (
    <div>
      <h1>WebTOC Options</h1>
      <p>Here you can configure the settings for the WebTOC extension.</p>
    </div>
  );
};

ReactDOM.render(<Options />, document.getElementById('root'));
